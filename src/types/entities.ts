// Centralized entity types for use throughout the project
import type { User, NewUser } from "../api/entities/user";
import type { RefreshToken, NewRefreshToken } from "../api/entities/refreshToken";
import type { VerificationToken, NewVerificationToken } from "../api/entities/verificationToken";

export type {
  User,
  NewUser,
  RefreshToken,
  NewRefreshToken,
  VerificationToken,
  NewVerificationToken,
};
